import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })
const playfair = Playfair_Display({ subsets: ['latin'], variable: '--font-playfair' })

export const metadata: Metadata = {
  title: 'Clasico Barbershop - Premium Grooming Experience',
  description: 'Experience the art of traditional barbering in a modern setting. Premium haircuts, beard grooming, and luxury services.',
  keywords: 'barbershop, haircut, beard grooming, luxury, traditional barbering',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} font-sans`}>
        {children}
      </body>
    </html>
  )
} 